# AGENTS.md — 图片智能重命名和分类项目

> 本文件供 AI 编码助手阅读。项目所有代码、注释及交互界面均使用中文。
> 当前版本：**v2.3.0**

---

## 版本号规则

初始版本为 1.6.0，其中 A.B.C 的命名规则如下：
1. C 表示 Bug 修改
2. B 表示小功能更新
3. A 表示大版本更新

**【AI 编码助手必读规范】**：
代码中通过全局变量 `VERSION` 声明当前版本，**AI 在进行任何代码修改后，都必须遵循上述规则自动递增并更新版本号，无需用户提示**。修改时请同步更新脚本顶部注释、`AGENTS.md` 以及 `CLAUDE.md`，并使用新版本号生成打包文件。

---

## 项目概述

本项目是一个基于**视觉语言模型（VLM）**的**图片智能重命名与自动分类**工具，自带 Vue 3 + FastAPI 构建的 Web 界面，用于对本地大量图片进行批量整理。

核心工作流：
1. 扫描配置的照片根目录，递归或单层收集待处理图片；
2. 利用多线程并行计算 MD5，跳过已处理过的文件（双重去重）；
3. 将图片压缩并转为 Base64，调用兼容 OpenAI 格式的 API（如豆包视觉模型）生成 JSON 格式的内容描述和分类建议；
4. 综合 VLM 分类建议、文件名强特征、比例检测进行最终分类决策；
5. 以描述作为新文件名，安全移动（重命名）图片到目标目录；
6. 记录处理日志并实时推送到 Web 前端，支持断点续跑和一键清除缓存。

项目包含三大核心组件：
- `main.py`（FastAPI 后端）：控制流入口，提供 RESTful API，并挂载 Vue 编译后的静态资源。
- `vlm_rename_v5.py`（处理引擎）：包含去重、VLM 接口调用、文件移动和多线程并发核心。
- `vlm_classify.py`（分类模块）：定义了分类体系和纯规则的迁移策略。

---

## 技术栈与运行环境

| 项目 | 说明 |
|------|------|
| 语言 | Python 3.10+ |
| 前端框架 | Vue 3 + Element Plus (位于 `frontend/` 目录) |
| 后端框架 | FastAPI + Uvicorn |
| 核心依赖 | `Pillow`（图像压缩）、`requests`（API 调用）、`platformdirs`（数据隔离） |
| IDE | IntelliJ IDEA / PyCharm |
| 操作系统 | Windows（为主，打包出 `.exe` 安装包），但代码逻辑需保持跨平台兼容 |

---

## 目录与数据架构隔离 (重点)

从 v2.1 版本开始，项目实现了**代码与数据的完全分离**：

1. **代码库 (`E:\Picture\_app`)**: 仅包含源代码、`.github` 工作流和前端工程。
2. **用户数据目录 (`USER_DATA_DIR`)**: 通过 `platformdirs.user_data_dir("Picture", "qingxuandaoming")` 动态获取，用于存放运行时产生的所有配置文件和日志（例如：`%LOCALAPPDATA%\qingxuandaoming\Picture`）：
   - `config.json`：存储 API 密钥和照片目标处理根目录 `base_dir`。
   - `_data/`：运行时日志、去重记录（`processed_md5.txt`）、缓存和配置（`categories.json`）。

> **AI 警告**：在修改读写配置文件的逻辑时，必须使用 `vlm_rename_v5.py` 或 `main.py` 中定义的 `USER_DATA_DIR` 和 `DATA_DIR`，**绝对不要**尝试在代码当前目录 (`./`) 下生成这些文件。

---

## 构建与运行命令

直接启动后端（会自动打开浏览器访问前端页面）：

```bash
cd _app
python main.py
```

前端开发模式（用于修改界面）：
```bash
cd _app/frontend
npm run dev
```

---

## 核心业务逻辑

### 1. 运行模式

支持通过 Web UI 传入 `run_mode` 参数：
- `1` → **默认模式**：分析重命名，并移动到分类目录。
- `2` → **仅重命名**：分析重命名，保留在原目录。
- `3` → **整理散落图片**：深度扫描（递归子目录）未分类的文件，移动到已有的有效子文件夹中。此模式拥有独立的 MD5 缓存记录以防冲突。

### 2. 双重去重系统

- `processed_files.txt`：兼容历史数据的纯文本记录。
- `processed_md5.txt`：基于 MD5 的精准去重。
支持在前端调用 `/api/system/clear_cache` 接口清除缓存，清除时会针对当前 `run_mode` 删除对应的缓存文件，强制重新触发 AI 分析。

### 3. 文件名安全与防冲突

- VLM 返回的描述会经正则过滤掉 Windows 非法字符。
- 若目标目录已存在同名文件，自动追加 `_1`、`_2` … 递增序号。

### 4. 分类决策流程

所有分类存储在 `categories.json` 中，前端可动态编辑。
**决策优先级**：比例检测（如横屏/1比1，最高优先）→ VLM直接分类 → 文件名强特征覆盖 → 关键词得分制 → 兜底（照片/其他）。

---

## 开发约定与测试

- **界面文本**：用户交互文本均以**中文**为主。
- **无状态后端**：前端通过轮询 `/api/batch/progress/{task_id}` 获取进度，任务运行于后台线程中，使用 `tasks_lock` 保证线程安全。
- **无自动化测试**：修改核心逻辑后，必须手动启动 `python main.py` 在浏览器中验证 API 和渲染表现，检查控制台报错。
- **打包发布**：修改代码并自动递增 `VERSION` 后，若需发布，推送到 GitHub 触发 Actions 进行 PyInstaller + Inno Setup 打包。
